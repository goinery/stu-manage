此目录包含 MySQL 初始化脚本。脚本会创建（或复用）`graduation_design` 数据库，
删除旧表后重新建表并写入演示数据。

```powershell
Get-Content -Raw -Encoding UTF8 sql/init.sql |
  docker exec -i goi_mysql mysql -uroot -p123456 --default-character-set=utf8mb4
```

应用启动命令：

```powershell
cd graduation-design-master
mvn spring-boot:run
```
