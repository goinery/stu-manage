# 学生选课管理系统验收演示

本文用于数据库课程设计验收。项目后端为 Spring Boot 2.2.6 + MyBatis + Thymeleaf，前端为 layui，数据库为 openGauss。本文覆盖数据库初始化、基础数据核验、管理员/教师/学生三类角色功能验收、核心业务 SQL 验收和常见问题排查。

## 1. 验收前提

- Maven 工程目录：`graduation-design-master/`
- 数据库初始化脚本：`graduation-design-master/sql/init_fixed.sql`
- 应用端口：`8088`
- 登录地址：`http://localhost:8088/`
- 本次代码侧已完成编译验证：`mvn clean package` 通过。
- 本次优化未连接数据库，数据库启动、初始化和最终验收由验收人执行。

## 2. 数据库初始化

在 openGauss 所在服务器上启动数据库：

```shell
su - omm
gs_om -t status --detail
gs_om -t start
gsql -d oasys -p 26000 -r
```

执行初始化脚本：

```sql
\i /path/to/init_fixed.sql
```

如果脚本路径在 Windows 本机，需要先将 `graduation-design-master/sql/init_fixed.sql` 放到数据库服务器可访问路径，或在 `gsql` 中粘贴执行。

脚本会先删除旧表，再创建并插入演示数据。当前脚本包含：

- 3 个学院
- 5 位教师
- 10 位学生
- 12 门课程
- 3 个学年
- 2 个选课阶段
- 26 条学生选课关系

## 3. 启动应用

从 Maven 工程目录执行：

```shell
cd graduation-design-master
mvn clean package
java -jar target/springboot-student.jar
```

模板快速调试可用：

```shell
mvn spring-boot:run
```

访问：

```text
http://localhost:8088/
```

## 4. 演示账号

| 角色 | 账号 | 密码 | 登录说明 |
| --- | --- | --- | --- |
| 管理员 | `admin` | `admin` | 登录角色选择“管理员” |
| 教师 | `叶凡` | `123456` | 登录角色选择“教师” |
| 学生 | `2025001` | `123456` | 登录角色选择“学生” |

学生 `2025001` 为完整演示账号，首页、选课中心、我的课程、数据分析均有数据。

## 5. 数据库基础核验 SQL

登录 `oasys` 数据库后执行：

```sql
SELECT COUNT(*) AS college_count FROM public.college;
SELECT COUNT(*) AS teacher_count FROM public.teacher;
SELECT COUNT(*) AS student_count FROM public.student;
SELECT COUNT(*) AS course_count FROM public.course;
SELECT COUNT(*) AS year_count FROM public.course_academic_year;
SELECT COUNT(*) AS stage_count FROM public.selection_stage;
SELECT COUNT(*) AS rel_count FROM public.student_course_rel;
```

预期结果：

| 项目 | 预期 |
| --- | --- |
| college_count | 3 |
| teacher_count | 5 |
| student_count | 10 |
| course_count | 12 |
| year_count | 3 |
| stage_count | 2 |
| rel_count | 26 |

核验当前启用学年：

```sql
SELECT id, academicYear, state
FROM public.course_academic_year
WHERE state='1';
```

预期：`2025上半学年`。

核验当前有效选课阶段：

```sql
SELECT id, stageName, allowRecommend, allowPlan, allowOutside, startDate, endDate
FROM public.selection_stage
WHERE academicYear='2025上半学年'
  AND state='1'
  AND startDate <= CURRENT_TIMESTAMP
  AND endDate >= CURRENT_TIMESTAMP
ORDER BY stageOrder
LIMIT 1;
```

预期：`第二阶段-全面开放`，三类选课均开放。

核验课程容量与选课关系一致：

```sql
SELECT c.id, c.courseName, c.selected, COUNT(sc.id) AS actual_selected
FROM public.course c
LEFT JOIN public.student_course_rel sc ON sc.courseId = c.id
GROUP BY c.id, c.courseName, c.selected
HAVING c.selected <> COUNT(sc.id);
```

预期：无返回行。

核验菜单权限：

```sql
SELECT r.name AS role_name, m.name AS menu_name, m.href
FROM public.role_menu_rel rm
JOIN public.role r ON r.id = rm.roleId
JOIN public.menu m ON m.id = rm.menuId
ORDER BY r.id, m.sort, m.id;
```

预期：管理员含教务/人员/公告/系统管理；教师含课程信息、学生信息、成绩管理、教师统计；学生含选课中心、已选课程、选课统计。

## 6. 数据库约束验收

本次优化后的 `init_fixed.sql` 已补充唯一约束、外键、CHECK 约束和常用索引。可执行：

```sql
\d public.student
\d public.teacher
\d public.course
\d public.student_course_rel
\di
```

重点检查：

- `student.studentNumber` 唯一
- `teacher.loginName` 唯一
- `student_course_rel(studentId, courseId)` 唯一
- `course.selected <= course.optional`
- `selectionType` 只能为 `recommend`、`plan`、`outside`
- 课程、学生、教师、角色、学院之间存在外键关系

重复选课约束测试：

```sql
BEGIN;
INSERT INTO public.student_course_rel
VALUES ('dup_test', 's001', 'c01', '1', '0', ' ', '0', 'recommend');
ROLLBACK;
```

预期：插入失败，提示唯一约束冲突。失败后执行 `ROLLBACK`，不影响演示数据。

非法容量约束测试：

```sql
BEGIN;
UPDATE public.course SET selected = optional + 1 WHERE id='c01';
ROLLBACK;
```

预期：更新失败，提示 CHECK 约束冲突。

## 7. 管理员功能验收

登录：`admin/admin`，角色选择“管理员”。

### 7.1 首页

预期：

- 显示管理员欢迎语。
- 显示管理员公告。
- 左侧菜单包含教务管理、人员管理、公告管理、系统管理。

### 7.2 课程列表

路径：教务管理 -> 课程列表。

验收点：

- 可看到 12 门课程。
- 按学院筛选有效。
- 按课程名称精确搜索有效。
- 修改课程名称、学分、可选人数、上课地点、上课时间、课程说明后提示成功。
- 删除未结束课程时应提示“当前课程还没有结束，无法删除”。

本次优化点：

- 课程更新 SQL 已改成单一 `SET`，避免多 `SET` 语法错误。
- 数值字段已改为可空包装类型，避免局部更新误写 `0`。
- 删除判断已修复 Java 字符串比较问题。

### 7.3 新增课程

路径：教务管理 -> 新增课程。

验收点：

- 不完整填写时，前端提示需要完整填写。
- 学分必须大于 0。
- 可选人数必须大于 0。
- 学院和教师不能停留在“请选择”。
- 成功新增后返回课程列表。
- 新课程自动归属当前启用学年。

### 7.4 学年管理

路径：教务管理 -> 学年管理。

验收点：

- 可查看 3 个学年。
- 切换启用学年后，仅一个学年为启用状态。
- 学年切换已加事务，避免中途失败导致全部关闭。

### 7.5 选课阶段管理

路径：教务管理 -> 选课管理。

验收点：

- 可查看 `第一阶段-推荐选课` 和 `第二阶段-全面开放`。
- 可切换阶段状态。
- 可切换推荐选课、方案内选课、方案外选课开放标记。
- 学生选课时应受当前有效阶段控制。

### 7.6 学院、教师、学生、角色、菜单、公告管理

验收点：

- 学院列表可查看并启用/禁用。
- 教师列表可按学院/姓名筛选并启用/禁用。
- 学生列表可按学院/姓名筛选并启用/禁用。
- 角色可分配菜单。
- 菜单树可查看。
- 公告可新增、查看、修改、删除。

本次优化点：

- 管理端登录判断改为 `userType=1`，非管理员访问不会出现类型转换异常。
- 角色菜单保存已加事务，避免删除旧菜单后插入失败造成角色菜单丢失。

## 8. 学生功能验收

登录：`2025001/123456`，角色选择“学生”。

### 8.1 首页

预期：

- 显示学生欢迎语。
- 显示已选课程数、已获得学分、当前学年。
- 显示学生公告。

### 8.2 选课中心

路径：选课中心。

预期：

- 顶部显示当前选课阶段。
- 当前种子数据在 `2026-06-26` 应命中 `第二阶段-全面开放`。
- 推荐选课、方案内选课、方案外选课均开放。

推荐选课：

- 显示软件工程学院、`2025上半学年` 的课程。
- 排除课程名包含“体育”的课程。
- 已选课程显示“退课”。
- 未选且有余额课程显示“选课志愿”。

方案内选课：

- 显示同学院其他学年的课程，如 `数据结构与算法`、`编译原理`、`计算机组成原理`。

方案外选课：

- 显示其他学院当前学年课程，如 `人工智能导论`、`高等数学A`。

本次优化点：

- 后端强制使用 session 中当前学生 ID，不信任前端传入 `studentId`。
- 后端校验课程是否属于当前 Tab 类型，不能伪造 `selectionType` 越权选择。
- 选课服务已加事务。
- 课程容量更新改成条件更新，避免超选。
- 插入选课关系失败时会回滚容量变化。

### 8.3 时间冲突和容量验收

可通过页面选择一门与已选课程时间重叠的课程，预期提示：

```text
上课时间冲突，无法选择该课程
```

可通过 SQL 将某课程容量临时改满后测试：

```sql
BEGIN;
UPDATE public.course SET optional = selected WHERE id='c10';
-- 页面尝试选择 c10 对应课程，应提示容量不足或已满
ROLLBACK;
```

### 8.4 我的课程

路径：我的课程 -> 已选课程。

预期：

- 显示 `2025上半学年` 已选课程。
- 显示课程名称、学院、教师、容量、学分、上课时间、上课地点、课程说明。
- 可退选课程。
- 退课后课程 `selected` 人数同步减少。

### 8.5 选课统计

路径：数据分析 -> 选课统计。

预期：

- 显示已完成课程统计。
- 显示正在进行课程统计。
- 显示已选课程统计。
- 显示推荐选课、方案内选课、方案外选课数量。
- 显示按学院统计。
- 显示时间冲突检测结果。

说明：图表使用 Chart.js CDN。若验收环境无法访问外网，页面会显示图表组件未加载提示，但统计卡片和接口数据仍可验收。

## 9. 教师功能验收

登录：`叶凡/123456`，角色选择“教师”。

### 9.1 首页

预期：

- 显示教师欢迎语。
- 显示所属学院。
- 显示当前学年。
- 显示教师公告。

### 9.2 课程信息

路径：课程管理 -> 课程信息。

预期：

- 显示叶凡在当前学年教授的课程。
- 显示课程名称、学院、教师、容量、已选人数、评分时间、上课时间、地点、说明。

### 9.3 学生信息

路径：课程管理 -> 学生信息。

验收点：

- 可按学年切换。
- 可按课程筛选。
- 可按学生姓名搜索。
- 表格显示学号、姓名、课程名、学年、课程结束时间。

本次优化点：

- 后端已接收并使用 `academicYear`、`courseName`、`username` 过滤条件。

### 9.4 成绩管理

路径：课程管理 -> 成绩管理。

验收点：

- 可按学年切换。
- 可按课程筛选。
- 可按学生姓名搜索。
- 未评价课程显示“成绩合格”和“成绩不合格”按钮。
- 不合格必须填写备注。
- 已评分记录不能重复评分。
- 课程未结束时不能评分。

本次优化点：

- 教师成绩表已查询 `student_course_rel.id`，评分按钮有真实记录 ID。
- 评分更新限制为当前教师自己的课程。
- 评分更新要求课程结束时间 `teachEndDate <= CURRENT_TIMESTAMP`。
- 评分更新要求记录当前未评分。

评分 SQL 核验：

```sql
SELECT sc.id, s.studentNumber, s.username, c.courseName, sc.state, sc.isQualified, sc.creditsRemark
FROM public.student_course_rel sc
JOIN public.student s ON s.id = sc.studentId
JOIN public.course c ON c.id = sc.courseId
WHERE c.teacherId='1'
ORDER BY c.courseName, s.studentNumber;
```

页面评分后，对应记录应变为：

- `state='1'`
- `isQualified='1'` 或 `isQualified='0'`
- 不合格时 `creditsRemark` 为教师填写内容

### 9.5 教师统计

路径：数据分析 -> 教师统计。

预期：

- 显示选课总人数。
- 显示授课课程数。
- 显示涉及学院数。
- 显示各学院选课人数列表。
- 图表组件可用时显示柱状图；断网时显示降级提示。

## 10. 个人资料与密码

三类角色右上角均有：

- 基本资料
- 安全设置
- 退出登录

验收点：

- 学生可修改手机号、邮箱。
- 教师可修改手机号、邮箱。
- 管理员可修改用户名。
- 修改密码需要验证旧密码。
- 密码修改成功后使用新密码登录。

说明：当前课程设计项目仍使用明文密码存储，适合演示环境。生产优化方向是 BCrypt 哈希、登录失败限制和验证码。

## 11. 推荐验收顺序

1. 执行 `init_fixed.sql` 完成数据库重置。
2. 执行第 5 节 SQL，确认基础数据数量正确。
3. 执行第 6 节 SQL，确认约束与索引存在。
4. 启动应用并登录管理员。
5. 验收管理员首页、课程列表、新增课程、学年管理、选课阶段管理。
6. 登录学生 `2025001`，验收首页、选课中心、我的课程、选课统计。
7. 登录教师 `叶凡`，验收课程信息、学生信息、成绩管理、教师统计。
8. 用 SQL 核验课程容量、选课关系、成绩记录是否与页面操作一致。

## 12. 常见问题排查

### 12.1 登录页提示数据库连接异常

说明数据库未启动、网络不可达、账号密码不匹配或 `application.properties` 配置不正确。

检查：

```shell
gs_om -t status --detail
```

核对：

```properties
spring.datasource.url
spring.datasource.username
spring.datasource.password
```

### 12.2 页面改了但 jar 运行没变化

Thymeleaf 模板会打进 jar。模板修改后使用 jar 运行，需要重新打包：

```shell
mvn clean package
java -jar target/springboot-student.jar
```

开发阶段可用：

```shell
mvn spring-boot:run
```

### 12.3 学生选课中心没有数据

检查：

```sql
SELECT * FROM public.course_academic_year WHERE state='1';
SELECT * FROM public.selection_stage WHERE state='1';
SELECT id, courseName, collegeId, academicYear, state FROM public.course ORDER BY academicYear, collegeId;
```

学生 `2025001` 属于学院 `1`，推荐选课需要学院 `1` 且当前学年 `2025上半学年` 的课程。

### 12.4 教师成绩管理没有可评分数据

检查教师 ID、学年和课程结束时间：

```sql
SELECT c.id, c.courseName, c.teacherId, c.academicYear, c.teachEndDate, COUNT(sc.id) AS student_count
FROM public.course c
LEFT JOIN public.student_course_rel sc ON sc.courseId = c.id
WHERE c.teacherId='1'
GROUP BY c.id, c.courseName, c.teacherId, c.academicYear, c.teachEndDate
ORDER BY c.academicYear, c.courseName;
```

只有 `teachEndDate <= CURRENT_TIMESTAMP` 且 `student_course_rel.state='0'` 的记录可以评分。

### 12.5 选课后人数不一致

执行：

```sql
SELECT c.id, c.courseName, c.selected, COUNT(sc.id) AS actual_selected
FROM public.course c
LEFT JOIN public.student_course_rel sc ON sc.courseId = c.id
GROUP BY c.id, c.courseName, c.selected
HAVING c.selected <> COUNT(sc.id);
```

预期无返回行。若有返回行，说明曾绕过应用直接修改过表数据，可根据 `COUNT(sc.id)` 修正 `course.selected`。

## 13. 本次优化摘要

- 修复教师成绩管理无法拿到选课关系 ID 的问题。
- 教师学生列表和成绩列表支持后端课程/学生/学年过滤。
- 教师评分后端校验当前教师、课程结束时间和评分状态。
- 修复课程更新 XML 多 `SET` 风险。
- 修复课程删除字符串比较问题。
- 课程数值字段改为包装类型，避免局部更新误写 `0`。
- 学生选课使用 session 学生 ID，不信任前端传入 ID。
- 学生选课校验课程类别与当前 Tab 是否匹配。
- 选课与退课加入事务。
- 课程容量更新加入条件，防止超选和负数。
- `init_fixed.sql` 补充外键、唯一约束、CHECK 约束和索引。
- 角色菜单保存加入事务。
- 文本型接口响应同时填充 `message` 和 `data`，兼容旧页面提示。
- Chart.js 断网时页面降级展示，不中断统计页面。
